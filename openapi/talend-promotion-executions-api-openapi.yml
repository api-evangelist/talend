openapi: 3.0.3
info:
  title: Talend Cloud Orchestration Artifacts Promotion Executions API
  description: The Talend Cloud Orchestration API manages tasks, plans, schedules, workspaces, environments, artifacts, connections, promotions, and resources in Qlik Talend Cloud. Use this API to automate data integration pipeline management, configure execution schedules, and manage workspace resources.
  version: 2021-03
  contact:
    name: Qlik Talend Support
    url: https://talend.qlik.dev
  termsOfService: https://www.qlik.com/us/legal/license-terms
servers:
- url: https://api.{region}.cloud.talend.com
  description: Talend Cloud API
  variables:
    region:
      description: Cloud region
      default: eu
      enum:
      - eu
      - us
      - us-west
      - ap
      - au
security:
- BearerAuth: []
tags:
- name: Promotion Executions
paths:
  /processing/executions/promotions:
    post:
      operationId: executePromotion
      summary: Execute Promotion
      description: Execute a promotion from source environment to target environment
      tags:
      - Promotion Executions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                promotionId:
                  type: string
                  description: The promotion to execute
      responses:
        '200':
          description: Promotion execution started
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Bearer token from Talend Cloud personal access token or service account token
externalDocs:
  description: Talend Orchestration API Documentation
  url: https://talend.qlik.dev/apis/orchestration/2021-03/
